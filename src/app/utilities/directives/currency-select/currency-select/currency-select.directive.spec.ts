import { CurrencySelectDirective } from './currency-select.directive';

describe('CurrencySelectDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencySelectDirective();
    expect(directive).toBeTruthy();
  });
});
