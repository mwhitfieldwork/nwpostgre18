import { CurrencySelectPipe } from './currency-select.pipe';

describe('CurrencySelectPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencySelectPipe();
    expect(pipe).toBeTruthy();
  });
});
